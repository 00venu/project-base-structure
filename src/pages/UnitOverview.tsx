import { classNames } from "./landingPage/classess";
const UnitOverview = () => {
  const { pageHeader } = classNames;
  return (
    <div>
      <h3 className={pageHeader}>UnitOverview</h3>
    </div>
  );
};

export default UnitOverview;
